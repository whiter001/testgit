#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../.1.0.1@crc-32/bin/crc32.njs" "$@"
  ret=$?
else 
  node  "$basedir/../../../.1.0.1@crc-32/bin/crc32.njs" "$@"
  ret=$?
fi
exit $ret
